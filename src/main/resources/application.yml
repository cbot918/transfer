spring:
  datasource:
    url: jdbc:mysql://localhost:3306/transfer?useSSL=false&serverTimezone=UTC
    username: yale
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

# Server port (optional, defaults to 8080)
server:
  port: 8080